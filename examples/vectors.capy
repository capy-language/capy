
core :: mod "core";

Vector3 :: distinct [3] i32;

main :: () {
    my_point : Vector3 = i32.[1, 2, 3];

    core.print("my_point: (");
    core.print(my_point[0]);
    core.print(", ");
    core.print(my_point[1]);
    core.print(", ");
    core.print(my_point[2]);
    core.println(")");
}
