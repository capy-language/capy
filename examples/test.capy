imaginary : type : distinct i8;

imaginary_ref : type : imaginary;

main :: () -> {
    i : distinct i8 = 1;
    x : distinct i8 = 1;

    printf("imaginary: %i\n", i + x);
};

printf :: (str: string, i: imaginary) -> {};