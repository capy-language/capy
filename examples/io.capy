
core :: mod "core";

label :: (label: str, val: core.Any) {
    core.print(label);
    core.println(val);
}

label_two :: (label1: str, val1: core.Any, label2: str, val2: core.Any) {
    core.print(label1);
    core.print(val1);
    core.print(label2);
    core.println(val2);
}

