
print_one :: (s: string, n: i32) {
    // libc's printf will ignore the extra arguments.
    // we have to do this because we can only define printf once
    printf(s, n, 0);
}

printf :: (s: string, n1: i32, n2: i32) extern;
